table:
  name: users
  schema: public
array_relationships:
  - name: post_users
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: post_user
          schema: public
